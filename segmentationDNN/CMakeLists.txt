cmake_minimum_required(VERSION 3.0.0)
project(segmentation VERSION 1.0.1 LANGUAGES CXX)
find_package(ITK REQUIRED)
include(${ITK_USE_FILE})
add_subdirectory(segmentation_dnn)
add_executable(${PROJECT_NAME} segmentation.cpp)
target_link_libraries(${PROJECT_NAME} segmentation_dnn)