cmake_minimum_required(VERSION 2.8.9)
project(algorithms_test)
add_subdirectory(hip)
add_subdirectory(implants)
add_subdirectory(registration)
add_subdirectory(segmentation)
add_subdirectory(load_files)
add_subdirectory(test)
#add_subdirectory(slicer)
add_subdirectory(CheckImageRod)
find_package(ITK REQUIRED)
include(${ITK_USE_FILE})
find_package(VTK REQUIRED)
include(${VTK_USE_FILE})
add_executable(${PROJECT_NAME} algorithms.cpp)
#target_link_libraries(${PROJECT_NAME} Hip Implants Registration Segmentation test)
target_link_libraries(${PROJECT_NAME} Hip Implants Registration Segmentation LoadFiles test CheckImageRod)