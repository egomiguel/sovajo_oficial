@PACKAGE_INIT@
include("${CMAKE_CURRENT_LIST_DIR}/THA_RegistrationTargets.cmake")


find_package(ITK REQUIRED)
include(${ITK_USE_FILE})

find_package(VTK REQUIRED)
include(${VTK_USE_FILE})

#find_package(OpenCV REQUIRED)

#set(Boost_COMPILER "-vc142")
#find_package(PCL 1.10 REQUIRED COMPONENTS io filters)

#target_link_libraries(Registration INTERFACE ${ITK_LIBRARIES} ${OpenCV_LIBS} ${PCL_LIBRARIES})

check_required_components(THA_Registration)
